#For reindexing & Deleting the json , HTML 
python sefs/main.py --reindex

#For JSON Visualization
python sefs/visualize.py

#To Run Project 
python sefs/main.py

#To install dependencies 
pip install -r requirements.txt

-----------------------------------------------------------------
#Move to root_folder
cd "C:\Users\atuku\New folder\sefs\root_folder"

#Generate text files 

for /L %i in (1,1,5) do echo Transformer attention mechanism %i improves stochastic gradient descent optimization in neural network backpropagation training. > ai_%i.txt

for /L %i in (6,1,10) do echo Inflation rate %i affects GDP growth and bond yield while central bank monetary policy controls fiscal deficit and stock exchange volatility. > finance_%i.txt

for /L %i in (11,1,15) do echo React frontend component %i manipulates DOM using JavaScript event listeners and REST API integration in browser rendering engine. > web_%i.txt

for /L %i in (16,1,20) do echo Kubernetes cluster %i orchestrates Docker containers across distributed virtual machines with autoscaling and load balancing infrastructure. > cloud_%i.txt

for /L %i in (21,1,25) do echo Clinical diagnosis case %i involves MRI imaging, cardiovascular pathology assessment and pharmacological hypertension treatment planning. > health_%i.txt
